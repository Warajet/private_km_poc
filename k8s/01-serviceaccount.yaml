apiVersion: v1
kind: ServiceAccount
metadata:
  name: private-km
  namespace: private-km
  labels:
    app.kubernetes.io/name: private-km
# Never auto-mount the Kubernetes API token — the app does not need it
automountServiceAccountToken: false
